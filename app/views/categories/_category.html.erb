<span>
  <%= category.name %>
  <%= link_to t('.edit'), edit_category_path(category) %>
<%= link_to t('.delete'), category, data: {
  turbo_method: :delete, turbo_confirm: t('common.confirm')
  } %>
<%= link_to t('common.back'), categories_path %>
</span>
